print("   ".isspace())  # True (only spaces)
print("\t\n".isspace()) # True (only whitespace characters)
print("Hello".isspace()) # False (contains letters)
print("  a  ".isspace()) # False (contains 'a')
